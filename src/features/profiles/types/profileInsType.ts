export interface ProfileIns {
  idPerfil:number
  nombrePerfil: string;
  descripcion: string;
  funciones: number[]; 
  mapas: number[]; 
  permisos: number[]; 
  proyectos: number[]; 
  zonas: string[]; 
}